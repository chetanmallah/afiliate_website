import { Button } from "@/components/ui/button";
import { useIntersectionObserver } from "@/hooks/use-intersection-observer";
import { ExternalLink, TrendingUp, Users, ShoppingCart, Store, Target, Zap } from "lucide-react";